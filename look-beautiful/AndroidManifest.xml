<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
      package="robot.arm"
      android:versionCode="1"
      android:versionName="1.0">
    <uses-sdk android:minSdkVersion="8" />

    <application android:icon="@drawable/icon" android:label="@string/app_name" android:theme="@style/XTheme">
        <activity android:name=".common.TabActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <activity android:name=".ModelActivity" />
        <activity android:name=".DesignActivity" />
        <activity android:name=".ActorCoverActivity" />
        <activity android:name=".MovieActivity" />
        <activity android:name=".ArtActivity" />
        
        <activity android:name=".ActorContentActivity" />
        
        <activity android:name=".TouchImageViewActivity" />

    </application>
</manifest>